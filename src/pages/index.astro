---
import MainLayout from "../layouts/MainLayout.astro";
const data = await fetch("https://jsonplaceholder.typicode.com/posts?_limit=5").then(data => data.json());
---

<MainLayout title="Home Page">
	<ul>
		{data.map(item => (
			<li>{item.title}</li>
		))}
	</ul>
</MainLayout>
